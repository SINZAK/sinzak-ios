# Swift Lint Rule Customize 
disabled_rules:
  - trailing_whitespace
  - unused_optional_binding
  - identifier_name
  - force_cast
  - function_body_length
  - empty_enum_arguments
  - implicit_getter
opt_in_rules:

included:
excluded:
  - Carthage
  - Pods
  
# rules
line_length:
  warning: 200
  error: 300
